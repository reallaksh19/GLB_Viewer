import { applyBakedRestraintGlyph } from './BakedRestraintGlyphBmCiiOdContactCorrected.js';

export function enhanceSupportDirectionProxy(object, comp = {}, options = {}) {
  return applyBakedRestraintGlyph(object, comp, options);
}
